const searchForm = document.querySelector('.search-form');
const inputRef = document.querySelector('.search-input');
const resetButtonRef = document.querySelector('.button');
const markupListRef = document.querySelector('.js-markup-list');

export { searchForm, inputRef, resetButtonRef, markupListRef };
